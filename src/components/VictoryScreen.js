const VictoryScreen = () => {
    return (
            <div style={{ display: "flex", flexDirection: "column", justifyContent: "center", alignItems: "center", textAlign: "center", minHeight: "100vh" }} className="dead-screen">
                <h2 style={{ color: "white" }}>VICTORY!</h2>
            </div>
        );
}

export default VictoryScreen;
